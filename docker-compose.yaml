services:
  aurum-db:
    image: postgres:18
    environment:
      POSTGRES_DB: aurum_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - aurum-db-data:/var/lib/postgresql
    ports:
      - '5432:5432'
volumes:
  aurum-db-data:
